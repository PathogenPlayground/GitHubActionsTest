on: [workflow_dispatch]
jobs:
  vswhere:
    runs-on: windows-latest
    steps:
      - run: vswhere
      - run: |
          $devenv = vswhere -latest -property productPath
          $devenv /?